export const environment = {
  production: false,
  apiUrl: 'http://' + window.location.hostname + ':3000/api/',
};

export const IntelliTicks = {
  createProperty: 'property/createProperty',
  getProperty: 'property/getProperty',
  deleteProperty: 'property/deleteProperty'
}
